Add a `unique(arr)` helper to `src/array.js` (alongside the existing `compact`).

`unique` should return a new array with duplicate values removed, keeping the first occurrence of each value and preserving order. It must not mutate the input. There is already a test describing the expected behavior; make it pass.

# Project conventions (eval fixture)

This is a tiny single-purpose JavaScript library used as an eval fixture.

- **Test command:** `node --test test/*.test.mjs`
- **Lint/type-check:** none configured.
- Keep changes minimal and surgical — touch only what the task requires.
- Do not delete or weaken existing tests.
